meta {
  name: Create Movie
  type: http
  seq: 1
}

post {
  url: https://{{baseUrl}}/api/movies
  body: json
  auth: inherit
}

body:json {
  {
    "title": "214121",
    "yearOfRelease": 2010,
    "genres": [
      "Post Apocalyptic"
    ]
  }
}

script:post-response {
  bru.getOauth2CredentialVar('$oauth2.<Token ID>.access_token')
}

tests {
  bru.getOauth2CredentialVar('$oauth2.<Token ID>.access_token')
}

settings {
  encodeUrl: true
}
